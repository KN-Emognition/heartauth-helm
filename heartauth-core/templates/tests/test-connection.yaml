apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "heartauth-core.fullname" . }}-test-connection"
  labels:
    {{- include "heartauth-core.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "heartauth-core.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
